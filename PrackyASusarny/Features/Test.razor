@page "/test"
@using Title = AntDesign.Charts.Title
<Line Data="data" Config="config"/>

@code{

    (string, int)[] data = new (string, int)[] {
        ("1991", 3)
    };

    LineConfig config = new LineConfig() {
        Title = new Title() {
            Visible = true,
            Text = "Line Graph",
        },
        Description = new Description() {
            Visible = true,
            Text = "Replace broken lines with smooth curves。",
        },
        Padding = "auto",
        ForceFit = true,
        XField = "year",
        YField = "value",
        Smooth = true,
    };

}