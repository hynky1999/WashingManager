@page "/{BEntity}/CreateReservation"
@using Microsoft.AspNetCore.Authorization
@using PrackyASusarny.Features.BEManagment
@using PrackyASusarny.Features.Reservations.Components
@attribute [Authorize]
@inject ILocalizationService Loc

<PageHeader Title="@Loc["Create Reservation"]"/>
<ModelLoader AllowedTypes="@ManagementConfig.AllowedEntities" Model="@BEntity">
    <Row Gutter="12">
        <Col Lg="12" Xs="24">
            <Title Level="4">@Loc["Manually Select"]</Title>
            <DynamicComponent Type="@(typeof(CreateReservation<>).MakeGenericType(context))"/>
        </Col>
        <Col Lg="12" Xs="24">
            <Title Level="4">@Loc["Suggest Time"]</Title>
            <DynamicComponent Type="@(typeof(SuggestReservation<>).MakeGenericType(context))"/>
        </Col>
    </Row>
</ModelLoader>

@code{

    [Parameter]
    public string? BEntity { get; set; }

}