@typeparam T
@using Castle.Core.Internal
@using System.ComponentModel
@inherits ComponentBase

@code{
    protected string EntityName = (typeof(T).GetAttribute<DisplayNameAttribute>()?.DisplayName ?? typeof(T).Name);
}