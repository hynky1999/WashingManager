@using System.Linq.Expressions
@inject ILocalizationService LocalizationService
<DatePicker @bind-Value="DateTimeValue" Disabled="Disabled" ShowTime="@showTime"></DatePicker>

@code {

    private DateTime? DateTimeValue {
        get => Value is null ? null : NodaUtils.ToDateTime((Instant) Value, LocalizationService);
        set {
            if (value is null) {
                Value = null;
            } else {
                Value = NodaUtils.ToInstant((DateTime) value, LocalizationService);
            }
            ValueChanged.InvokeAsync(Value);
        }
    }

    [Parameter]
    // ReSharper disable once InconsistentNaming
    public bool showTime { get; set; }

    [Parameter]
    public Instant? Value { get; set; }

    [Parameter]
    public EventCallback<Instant?> ValueChanged { get; set; }

    [Parameter]
    public Expression<Func<Instant?>>? ValueExpression { get; set; }

    [Parameter]
    public bool Disabled { get; set; }

}